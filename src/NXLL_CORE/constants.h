
#include <stdint.h>
// #include <assert.h>
#include <cassert>

typedef int8_t i8;
typedef int16_t i16;
typedef int32_t i32;
typedef int64_t i64;

typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
typedef uint64_t u64;

typedef float f32;
typedef double f64;

// void assert_types()
//{
//     assert(sizeof(i8) == 1 && "[ERROR]: Size of uint8_t must be 1 byte.\n");
//     assert(sizeof(i16) == 2 && "[ERROR]: Size of uint16_t must be 2 bytes.\n");
//     assert(sizeof(i32) == 4 && "[ERROR]: Size of uint32_t must be 4 bytes.\n");
//     assert(sizeof(i64) == 8 && "[ERROR]: Size of uint64_t must be 8 bytes.\n");
//     assert(sizeof(u8) == 1 && "[ERROR]: Size of uint8_t must be 1 byte.\n");
//     assert(sizeof(u16) == 2 && "[ERROR]: Size of uint16_t must be 2 bytes.\n");
//     assert(sizeof(u32) == 4 && "[ERROR]: Size of uint32_t must be 4 bytes.\n");
//     assert(sizeof(u64) == 8 && "[ERROR]: Size of uint64_t must be 8 bytes.\n");
//     assert(sizeof(f32) == 4 && "[ERROR]: Size of float must be 4 bytes.\n");
//     assert(sizeof(f64) == 8 && "[ERROR]: Size of double must be 8 bytes.\n");
// }
